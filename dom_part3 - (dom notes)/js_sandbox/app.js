//What is the DOM

// A structured representation of a HTML document
// A tree of nodes/elements created by the browser
// Object orientated so nodes have properties and methods which can be mutated
// Contained within the document object
// All nodes on DOM can be manipulated with JS